#define _CRT_NO_SECURE_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(void) {
	char str[100];
	int n=0,cnt=0,temp=0,order=0;
	scanf("%s", str);
	str[strlen(str) - 1] = 0;
	int len = strlen(str);
	while (str[n] != 0) {
		
	}
	return 0;
}